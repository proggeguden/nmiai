# Round 24: Remove Harmful Handlers + Fix Root Causes

> **For agentic workers:** REQUIRED: Use superpowers-extended-cc:subagent-driven-development (if subagents available) or superpowers-extended-cc:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove deterministic error handlers that cause cascade failures and efficiency loss. Fix the planner to handle products and accounts correctly without runtime recovery.

**Architecture:** Remove product and account duplicate handlers from executor. Fix planner prompt to GET-before-POST for products. Revert account strategy to GET-first. Clean up dead code.

**Tech Stack:** Python, FastAPI, Gemini LLM planner

---

### Task 1: Remove duplicate product handler

**Why:** When POST /product/list fails 422, the handler searches for only 1 product but the plan expects `values[0..N]`. This corrupts the result structure and cascades failures to order + invoice steps. Removing it = clean fail-fast, 1 error instead of 3+.

**Files:**
- Modify: `agent.py` lines 1599-1639

- [ ] **Step 1: Remove the duplicate product handler block**

In `agent.py`, delete the entire block from `# ── Deterministic fix: duplicate product number` through the matching return statement (lines 1599-1639). This is the block that checks for `"produktnummeret"` and `"er i bruk"` in the error.

- [ ] **Step 2: Remove the orphaned dead code line**

Delete the orphaned `_log_self_heal(...)` call at approximately line 1646 (after the product handler). This references an undefined `fixed_args` variable and is dead code.

- [ ] **Step 3: Verify import works**

Run: `python3 -c "from main import app; print('OK')"`

---

### Task 2: Remove duplicate account handler

**Why:** Each 422 on POST /ledger/account costs efficiency points. The handler recovers via free GET, but the initial 422 still hurts scoring. Better to GET first in the plan (free) and POST only when needed.

**Files:**
- Modify: `agent.py` lines 1564-1597

- [ ] **Step 1: Remove the duplicate account handler block**

In `agent.py`, delete the entire block from `# ── Deterministic fix: duplicate ledger account number` through the matching return statement (lines 1564-1597).

- [ ] **Step 2: Verify import works**

Run: `python3 -c "from main import app; print('OK')"`

---

### Task 3: Fix planner prompt — account strategy

**Why:** After removing the account handler, the planner must handle accounts correctly. The current prompt says "always CREATE directly" which will now fail if accounts exist. Revert to GET-first strategy.

**Files:**
- Modify: `prompts.py`

- [ ] **Step 1: Update account rule in prompts.py**

Replace the ledger accounts rule with:
```
- **Ledger accounts**: Standard accounts (1920, 1500, 2400, 6010, 6500, etc.) usually exist. ALWAYS GET /ledger/account?number=NNNN first. Use the GET result ID in your voucher postings ($step_N.values[0].id). Only POST /ledger/account if you need a non-standard account that is unlikely to exist (e.g. 1209, 6700, 8700, 2920) — and GET those first too since they may have been created by a prior task.
```

- [ ] **Step 2: Verify prompt renders**

Run: `python3 -c "from prompts import PLANNER_PROMPT; PLANNER_PROMPT.format(today='2026-03-21', tool_summaries='test', task='test'); print('OK')"`

---

### Task 4: Fix planner prompt — product search-before-create

**Why:** Products with task-specified numbers often already exist in production. The planner must GET first and only POST if the product doesn't exist. This avoids 422s entirely (GETs are free).

**Files:**
- Modify: `prompts.py`

- [ ] **Step 1: Update Example 5 (Order→Invoice→Payment) to show GET-first for products**

Replace the Example 5 product creation step with a GET-first pattern:
```json
{"step_number": 2, "tool_name": "call_api", "args": {"method": "GET", "path": "/product", "query_params": {"number": "5001", "count": 1}}, "description": "Search for product by number (may already exist)"},
{"step_number": 3, "tool_name": "call_api", "args": {"method": "POST", "path": "/product", "body": {"name": "Consulting", "number": "5001", "priceExcludingVatCurrency": 25000.0, "vatType": {"id": 3}}}, "description": "Create product if not found (will be skipped if step 2 found it)"},
```

And update downstream refs to use ternary: `$step_2.values.length > 0 ? $step_2.values[0].id : $step_3.value.id`

**Note:** This uses the executor's existing ternary conditional support.

- [ ] **Step 2: Verify prompt renders**

Run: `python3 -c "from prompts import PLANNER_PROMPT; PLANNER_PROMPT.format(today='2026-03-21', tool_summaries='test', task='test'); print('OK')"`

---

### Task 5: Fix project activity linkage (422)

**Why:** POST /project/projectActivity fails with 422 in project lifecycle tasks. The activity must be created with correct activityType and linked properly.

**Files:**
- Modify: `prompts.py`

- [ ] **Step 1: Check endpoint catalog for /project/projectActivity requirements**

Run: `python3 -c "from endpoint_catalog import ENDPOINT_CARDS; import json; c = ENDPOINT_CARDS.get('POST /project/projectActivity', {}); print(json.dumps(c, indent=2)[:2000])"`

- [ ] **Step 2: Update prompt guidance for activity→project linkage based on findings**

The rule should specify exact required fields and correct activityType enum value.

---

### Task 6: Fix occupation code lookup (404)

**Why:** GET /salary/occupationCode returns 404 — the endpoint path is wrong. Employment details can't set occupationCode without a valid lookup.

**Files:**
- Modify: `prompts.py`

- [ ] **Step 1: Find the correct occupation code endpoint**

Run: `python3 -c "from endpoint_catalog import ENDPOINT_INDEX; matches = [k for k in ENDPOINT_INDEX if 'occupation' in k.lower()]; print(matches)"`

- [ ] **Step 2: Update prompt with correct endpoint path**

---

### Task 7: Smoke test, commit, and deploy

**Files:**
- All modified files

- [ ] **Step 1: Final smoke test**

Run: `python3 -c "from main import app; print('OK')"`

- [ ] **Step 2: Run validate_plan test**

```python
python3 -c "
from agent import validate_plan
plan = [{'step_number': 1, 'tool_name': 'call_api', 'args': {'method': 'POST', 'path': '/ledger/voucher', 'body': {'date': '2026-03-21', 'postings': [{'account': {'id': 1}, 'amount': 100}]}}}]
result = validate_plan(plan)
assert result[0]['args']['body']['postings'][0].get('amountGross') == 100
print('validate_plan OK')
"
```

- [ ] **Step 3: Commit**

```bash
git add agent.py prompts.py
git commit -m "Round 24: Remove harmful handlers, fix planner for products/accounts/activities"
```

- [ ] **Step 4: Deploy**

```bash
gcloud builds submit --tag gcr.io/ai-nm26osl-1788/tripletex
gcloud run deploy tripletex --image gcr.io/ai-nm26osl-1788/tripletex --platform managed --region europe-west1 --allow-unauthenticated --min-instances=3 --concurrency=1 --update-env-vars GEMINI_PLANNER_MODEL=gemini-3.1-pro-preview,GEMINI_MODEL=gemini-3-flash-preview
```

- [ ] **Step 5: Submit for scoring**

Submit at https://app.ainm.no/submit/tripletex
