# Round 35: Remove Replan + Performance + API Fixes

> **For agentic workers:** Execute tasks sequentially, verify after each.

**Goal:** Remove slow replan, boost Cloud Run performance, fix 7 specific API issues from manual submission testing.

**Architecture:** Remove replan from agent.py, add validate_plan fixes, update prompts, upgrade Cloud Run config, add requests.Session pooling.

---

### Task 1: Remove replan (it burns 150-300s per attempt)
- Modify: `agent.py` — remove the entire replan block added in Round 34
- [ ] Delete the replan code between "Single-attempt LLM replan" and "All other errors"
- [ ] Verify: `python3 -c "from main import app; print('OK')"`

### Task 2: Fix `/timesheetEntry` → `/timesheet/entry` hallucination
- Modify: `agent.py` validate_plan — add path correction
- [ ] Add rule: if path contains `/timesheetEntry`, replace with `/timesheet/entry`
- [ ] Verify

### Task 3: Fix employment/details — inject missing `date` field
- Modify: `agent.py` validate_plan
- [ ] If POST /employee/employment/details body is missing `date`, inject from employment startDate or today
- [ ] Verify

### Task 4: Add `paidAmountCurrency` guidance for foreign currency invoices
- Modify: `prompts.py`
- [ ] Add to API Constraints: "For foreign currency invoices: PUT /:payment needs BOTH paidAmount (NOK) AND paidAmountCurrency (foreign amount). Tripletex auto-calculates agio/disagio."
- [ ] Verify prompt renders

### Task 5: Add activity/timesheet flow guidance
- Modify: `prompts.py`
- [ ] Add: "Timesheet: POST /timesheet/entry (NOT /timesheetEntry). Use PROJECT_GENERAL_ACTIVITY for project timesheets."
- [ ] Verify

### Task 6: Strip read-only supplier fields + fix supplier guidance
- Modify: `agent.py` validate_plan
- [ ] Strip `isSupplier`, `isWholesaler`, `displayName`, `locale` from POST /supplier body
- [ ] Verify

### Task 7: Add requests.Session pooling for Tripletex API
- Modify: `tools.py`
- [ ] Replace bare `requests.get/post/put/delete` with session-based calls
- [ ] Add timeout=30 on all HTTP calls
- [ ] Verify

### Task 8: Verify all, commit
- [ ] Full smoke test
- [ ] Commit (do NOT deploy — wait for user permission)
- [ ] Print deploy command with upgraded Cloud Run config
